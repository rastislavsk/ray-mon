{
  "name": "ray-mon-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
